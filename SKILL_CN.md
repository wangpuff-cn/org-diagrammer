---
name: org-diagrammer
description: >
  生成交互式股权架构图 / 组织架构图（独立 HTML 或对话内 Widget）的专用 skill。
  当用户提到"股权架构图"、"股权结构图"、"股权穿透图"、"持股关系图"、"画股权"、
  "公司架构图"、"VIE 结构图"、"出资结构"、"股东关系图"、"组织架构图"、"部门架构图"、
  "组织结构图"、"汇报关系图"时使用本 skill。
  输入为标准 Excel 模版（股权模版沿用 equity-chart 的 12 列表格；组织模版见
  assets/组织架构图填写模版.xlsx），输出可交互 HTML：折叠/展开分支、黑白⇄彩色、
  缩放（含适应窗口）、搜索高亮定位、导出 SVG/PNG/PDF、节点悬停展示详情。
  与 equity-chart（matplotlib 静态 PNG）的区别：本 skill 产出的是可交互的 Web 版本。
---

# org-diagrammer · 交互式股权/组织架构图

## 能做什么

- **双模式**：股权架构图（equity）+ 组织架构图（org），一份 Excel 进、一张交互图出。
- **交互能力**（前端模板自带，无需额外开发）：
  - 节点底边徽章点击 **折叠/展开分支**，折叠后自动重排恢复正常间距，被操作节点保持可见
  - 工具栏：黑白⇄彩色、层级折叠下拉（全部展开/全部折叠/折叠至 N 层）、缩放下拉（适应窗口 + 50%~1000%）、＋/− 步进缩放（已禁用手势缩放防误触）
  - 搜索框：输入即出结果下拉（最多 10 条），选中后高亮目标及其**向上穿透至顶层**的链路（下层不高亮、其余变淡；点击节点则为双向穿透），并以"目标+上层节点"宽度放大定位（≤500%）
  - 导出文件下拉：SVG / PNG / PDF
  - 悬停节点：股权节点显示股东与比例；组织节点显示 职能/负责人/人数/上级
- **视觉规范**（与 equity-chart 一致）：圆角长方形节点、正交折线（干线在上下层间居中）、实线=持股/直接管理、虚线=协议控制/虚线汇报、默认黑白线框（有填充色时默认彩色）、上市主体加粗边框。

## 目录结构

```
org-diagrammer/
├── SKILL.md                          ← 本文件
├── scripts/
│   └── build_equity_json.py          ← 核心：Excel → 布局 JSON / 独立 HTML（依赖 openpyxl）
└── assets/
    ├── 股权架构图填写模版.xlsx        ← 股权模式填写模版（13 列，含示例与填写说明）
    ├── 组织架构图填写模版.xlsx        ← 组织模式填写模版（含示例数据）
    └── widget-template/
        └── index.html                ← 前端交互模板（含 /*__EQUITY_DATA__*/ 与 /*__CHART_TITLE__*/ 占位符）
```

股权模式输入模版见 `assets/股权架构图填写模版.xlsx`（13 列，含 主体业务及职能 列，详见下文解析规则）。

## 标准用法（一条命令）

```bash
python3 scripts/build_equity_json.py 输入.xlsx \
    --title "XX公司股权架构图" \
    --html 输出路径/MMDD_XX公司股权架构图.html
```

- `--mode auto`（默认）按 sheet 名/表头自动判定 equity 或 org，也可显式 `--mode equity|org`
- `--json out.json`：只输出布局 JSON（想把数据接到别的前端时用）
- 不带 `--json/--html` 时 JSON 打到 stdout
- 模板路径默认取 `<script_dir>/../assets/widget-template/index.html`，可用 `--template` 覆盖

输出 HTML 是**完全独立的可分享文件**（数据内联、无外部依赖），浏览器直接打开即可交互，标签页标题 = `--title`。

## 两种 Excel 模版的解析规则

### 股权模版（equity）

- 模版见 `assets/股权架构图填写模版.xlsx`（13 列，含三个 sheet：绘图用/示例/填写说明）
- 表头按**列名匹配**（容忍列顺序调整）：节点颜色/层级编号/公司名称/国家/省/市/区/股权关系/境内·境外/股东·母公司/持股比例/主体业务及职能/特殊要求
- 国家/省/市/区拼成节点第二行地区文字（`-`/`——` 跳过），地区行字号 12pt
- **主体业务及职能**：悬停 tooltip 展示；内容含"上市"时该主体按上市主体处理（加粗边框）
- 股权关系：`协议控制`→虚线，其余（直接持股/直接控股）→实线
- 股东/母公司支持 `/` 分隔多股东，持股比例一一对应（fan-in 汇线）
- 节点颜色列=**单元格填充色**（主题色+tint 自动还原）；填色→该节点用该色、默认彩色模式，未填→白底黑框
- 特殊要求列：自由文本，已支持"境内主体低于境外主体"（境内整体下沉）
- 同一母公司同层子主体 **>10 个自动换行**（有下层分支的放靠下行）

### 组织模版（org）

- 表头（assets 内模版为准）：节点颜色/层级编号/部门·组织名称/负责人/组织职能/人数/管理关系/上级部门·组织/特殊要求
- 上级列支持公式（如 `=C$2`，读 data_only 缓存值，无缓存回退）
- 管理关系：含"虚线/兼管/协议"→虚线，其余→实线
- 人数列支持公式（SUMIFS 等）；若模版拆分为 正编/外包 两列则分别读取
- 职能/负责人/人数/上级**不画在节点上**，以悬停 tooltip 展示
- 同层节点等宽（以该层最长名称为准、紧凑贴合），同层 **>15 个自动换行**

## 布局与视觉参数（scripts 内可调）

`H_UNIT=42`（节点高）、名称字号 14pt（自适应缩至 6pt 保底，不换行）、地区行 12pt、
宽度**同一层级等宽**：以该层最长名称节点的实际需求宽度为准应用到全层，两边留白仅 `NODE_PAD≈5px`（贴合文字，不量化取档）、
层间距 一对一=1×高 / fan-in·out=2×高、节点间距 `0.6×H_UNIT`。
机制（tidy-tree 父居中于子、兄弟组换行、干线居中、正交折线）不要改；数值可随规模微调。

## 在 Kimi Work 中作为 Widget 展示（可选）

默认交付独立 HTML 即可。若用户希望直接出现在对话/看板里：
1. `Widget` 工具创建 Widget，把 `assets/widget-template/index.html` 内容写入其 `workspace/index.html`
2. 用脚本 `--json` 产出的数据替换 `/*__EQUITY_DATA__*/ null`，标题替换 `/*__CHART_TITLE__*/架构图`
3. `Widget.show` 展示。**不要**把整段 HTML 贴进回复正文。

## 注意事项

- 依赖仅 `openpyxl`（Kimi Work 托管 Python 自带）；读颜色不能用 `data_only=True` 单开，须双开（样式+缓存值）
- 边两端必须在节点表内，悬空引用自动丢弃
- 画布 viewBox 由节点实际边界计算，不裁切
- 修改模板 `index.html` 后务必保留两个占位符注释，否则注入失败
